---
"demo-sdk": patch
---

Commit-based fallback and reliable CI triggers; add farewell() helper; resolve repo root from scripts/lib correctly; let pnpm/action-setup read version from packageManager; add uppercase option to greet()
