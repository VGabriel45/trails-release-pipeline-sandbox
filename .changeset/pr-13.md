---
"@vgabriel45/demo-utils": patch
---

`slugify` now returns `"untitled"` instead of an empty string for symbol-only or whitespace input, `clamp` safely handles non-numeric values, and `truncate` coerces nullish inputs to strings.
